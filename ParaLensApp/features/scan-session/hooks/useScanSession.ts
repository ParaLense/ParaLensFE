// Placeholder hook for future session-wide state (e.g. sharing OCR snapshot
// between camera and review screens). For now this is a minimal stub so that
// the scan-session feature exposes a clear entry point.
export function useScanSession() {
  return {};
}

